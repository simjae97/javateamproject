
프로젝트
    src- Application(클래스)
        -lib(패키지) + 외부 라이브러리를 모아두는 곳
        - model(패키지)
            -dao(패키지)
                ~ 클래스
            -dto(패키지)
                ~클래스
        - view(패키지)
            ~클래스
        - controller(패키지)
            ~클래스.